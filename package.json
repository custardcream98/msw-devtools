{
  "name": "han-type",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky",
    "type-check": "pnpm --filter \"./{packages,apps}/**\" type-check",
    "start:web": "pnpm --filter web dev",
    "build:sample-react-package": "pnpm --filter sample-react-package build"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "typescript": "^5.4.5"
  }
}
