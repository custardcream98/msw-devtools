{
  "$schema": "https://json.schemastore.org/package",
  "name": "msw-devtool",
  "scripts": {
    "format": "prettier --write .",
    "lint": "pnpm --filter \"./{packages,apps}/**\" lint",
    "lint:fix": "pnpm --filter \"./{packages,apps}/**\" lint:fix",
    "type-check": "pnpm --filter \"./{packages,apps}/**\" type-check",
    "prepare": "husky",
    "dev": "concurrently -k -s first -n \"PACKAGE,PLAYGROUND\" -c \"magenta,blue\" \"pnpm --filter @custardcream/msw-devtool build:watch\" \"pnpm --filter playground dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "typescript": "^5.4.5"
  }
}
